Create an AI analyzer for Docker inspection results. It should identify security, reliability, and cleanup findings such as privileged containers, exposed ports, missing restart policies, high CPU or memory usage, stopped containers, dangling images, unused custom networks, and potential secret environment variable keys. Return strict JSON with executive_summary, risk_level, issues, and safe_cleanup_notes.
